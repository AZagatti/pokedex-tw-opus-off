# Build Journal — Pokédex

Running log of anything that looked wrong, rework, confusion, missing context, surprises. One dated bullet per event.

## 2026-07-08

- Session start. Read SPEC.md. Scope: full SvelteKit 5 Pokédex on PokeAPI, adapter-static SPA, Tailwind v4, zod, ultracite(oxlint+oxfmt), lefthook, vitest+Playwright, GH Actions → Pages, Lighthouse ≥90. Remote: `https://github.com/AZagatti/pokedex-tw-opus-off.git`, base `/pokedex-tw-opus-off`.
- Env: node v24.17.0, npm 11.17.0, git 2.43.0, gh 2.92.0. Not a git repo yet.
- Surprise: `sv create` scaffold (v0.16.2, vite-plugin-svelte v7) does NOT generate `svelte.config.js` — it inlines `compilerOptions`/`adapter` into `vite.config.ts`. Confirmed via context7 that kit config (adapter/paths/prerender) still belongs in `svelte.config.js`, so I created one and reduced vite.config to `[tailwindcss(), sveltekit()]`. Build reads svelte.config.js correctly (base path applied to 404.html). Standard path still works.
- npm 11 `allow-scripts` gate blocked lefthook's postinstall (binary didn't install). Fixed with `npm approve-scripts lefthook` → adds `allowScripts` to package.json.
- `lucide-svelte` latest resolved to `1.0.1` (jump from old 0.x line) — noting in case of API differences with Svelte 5. Will verify when first used.
- `npx ultracite init --linter oxlint` generated `oxlint.config.ts` importing `ultracite/oxlint/sveltekit` — that preset does NOT exist (ERR_MODULE_NOT_FOUND). Available presets are `core`, `svelte`, `vitest`, etc. Fixed to import `svelte` + `vitest`.
- ultracite oxlint `core` preset enables very strict stylistic rules: `sort-keys` (fights logical config ordering) and `prefer-const` (misfires on Svelte 5 `let {..} = $props()`). Turned `sort-keys` off globally and `prefer-const` off for `**/*.svelte`; disabled `require-module-specifiers` for `**/*.d.ts` (needs `export {}`). Documented in DECISIONS.md later.
- vite.config `test` key needs `defineConfig` imported from `vitest/config` (not `vite`) or svelte-check errors on unknown property.
- Task 1 DONE: lint/format/check/build all green, lefthook installed & ran green on first commit (adc0c76).
- Confusion: first `git commit` of API layer silently "passed hooks" in my truncated output but did NOT create a commit — the lefthook pre-commit oxlint (run on staged files) actually FAILED; I'd only run `npm run check` (typecheck) after writing the API files, never `npm run lint`, so I missed real lint errors. Lesson: run `npm run lint` after writing code, not just typecheck. lefthook correctly blocked the bad commit.
- ultracite core preset also enables `func-style: expression` (forces every function into an arrow const). Pervasive; disabled it (function declarations hoist + cleaner generics). Fixed the legit rules instead: `require-unicode-regexp` (added `/u`), `prefer-string-replace-all` (replaceAll), `consistent-type-specifier-style` (split `import type`). All documented for DECISIONS.md.
- Task 2 DONE: schemas validated against LIVE PokeAPI (pikachu/pichu evo, cheri berry, fire type all parse).
- BIG runtime bug found via live screenshot: sprite images 429'd (rate-limited) because PokeAPI's sprite URLs point at `raw.githubusercontent.com`, which throttles bursts (30 cards → 30 requests). Fix: rewrite sprite URLs to the **jsDelivr CDN mirror** (`cdn.jsdelivr.net/gh/PokeAPI/sprites@master/...`) via `toCdn()`. All sprites load now + faster for Lighthouse. Updated preconnect too.
- More ultracite `core` rules surfaced as I added UI code (linting is incremental per file). Fixed the legit ones in code: `no-array-sort`→`toSorted`, `consistent-type-specifier-style` (split type imports), `prefer-destructuring`, `catch-error-name` (err→error), `no-duplicate-imports`. Disabled stylistic/convention-clashing ones: `prefer-await-to-then` + `prefer-await-to-callbacks` (promise chains are idiomatic inside `$effect` where cleanup must be sync), and `unicorn/filename-case` for `**/*.svelte` (Svelte components are PascalCase by universal convention — kebab-case would fight the ecosystem). All documented for DECISIONS.md.
- Verified LIVE via Playwright screenshots: list grid, search ("char"→10/1351), Fire+Gen-I combined filter (→12, exactly right), dark theme toggle, favorite heart (+nav badge). Looks polished. Task 4 functionally DONE.
- Task 5 (detail) built + verified LIVE: charizard hero/artwork/stats/abilities(hidden tag)/moves/evolution(Charmander→16→Charmeleon→36→Charizard)/sprite-variant switcher (shiny works, pixelated). 0 console errors.
- Subagent review (shell+list) returned 2 REAL bugs + a11y issues — all fixed:
  1. `PokedexToolbar` search box went stale when filters cleared externally (whole `filters` obj replaced) — added a `lastSynced` re-sync effect.
  2. `cache.ts` in-flight `finally` deleted by key not identity → a racing `force:true` call could evict a newer request. Fixed with identity check (`inflight.get(url)===request`).
  3. WCAG contrast: white text on light type colors (electric/ice/ground) failed AA. Added `readableTextOn()` (luminance→black/white, threshold 0.2 verified per-type) + gentler badge gradient; applied to TypeBadge + toolbar chips. Verified live (electric now dark-on-yellow).
  4. `--text-faint` on dex numbers → bumped to `--text-muted`.
  5. Tap targets: theme-toggle 38→44px, favorite 40px min.
  6. Heading order h1→h3 skip → PokemonCard name is now `<h2>`. Reviewer confirmed no reactivity loops, correct deep-$state, SSR guards, cleanup, reduced-motion all good.
- BIG surprise: SPEC pins `lucide-svelte`, but `lucide-svelte@1.0.1` is a **deprecation stub** ("Package deprecated. Please use @lucide/svelte instead.") with only a handful of working icons — `Github` etc. don't resolve, svelte-check errored. The real Svelte-5 package is `@lucide/svelte` (v1.23.0, 1745 icons). Switched to it (same import API, `import { Heart } from "@lucide/svelte"`). Kept spec intent (lucide icons). NOTE: lucide dropped brand icons, so GitHub mark is inlined as SVG; `filter` renamed → used other icons. Documented for DECISIONS.md.
- ROOT-CAUSED a recurring "commit silently doesn't happen" gotcha: `oxfmt` does NOT format `.svelte` files. The lefthook pre-commit oxfmt glob included `svelte`, so a commit staging ONLY `.svelte` files handed oxfmt a lone svelte file, which it excludes → "Expected at least one target file" → exit 2 → commit blocked (and my `tail`-truncated output hid it). The favorites commit (only +page.svelte) hit this. Fix: dropped `svelte` from the oxfmt glob (kept it on oxlint, which lints svelte <script>). Also gitignored `.playwright-mcp/` (browser MCP snapshot dumps were getting staged).
- Tests: 35 vitest unit (cache w/ mocked fetch incl. in-flight dedup, schemas, pokedex filter/sort/evolution, format, constants incl. contrast, favorites rune store) + 7 Playwright e2e (list/search/gen-filter/detail+evolution/favorite-persist-reload/theme-persist/berries). All pass.
- Dropped the `ultracite/oxlint/vitest` preset after 3 failed attempts to scope it: its rules can't be turned off per-file via `files` overrides (the `vitest/`-prefixed rule ids in overrides were no-ops), AND it misfires on Playwright e2e specs — `vitest/prefer-importing-vitest-globals` literally demanded importing vitest globals into `@playwright/test` files, and `consistent-test-filename` rejected `.spec.ts`. Core+svelte still lint tests. Complied with the vitest style rules that DID fire on unit tests (toStrictEqual, toBeTruthy/Falsy) before removing the preset. Documented for DECISIONS.md.
- Berries/favorites/detail all built + verified LIVE (berry detail flavors+facts; wiki-berry jsDelivr 403 → auto-fell-back to raw GitHub via new PokemonImage fallback; favorites persist + reactive removal → empty state). Tasks 6 & 7 DONE.
- SCARE (self-inflicted, resolved): I ran `cd node_modules/lucide-svelte/dist/icons` in one Bash call; the tool's CWD **persists across calls**, so the next several commands (grep package.json, npm uninstall/install, git checkout) all executed _inside node_modules_ — made it look like root package.json was corrupted to lucide's manifest and svelte downgraded to 4. Nothing was actually broken; root had svelte 5.56.4 all along. Fix: `cd` back to project root; going forward use absolute paths for node_modules inspection, never leave CWD in a subdir.
- CI failed on the docs push: `npm run format:check` (`oxfmt --check .`, whole repo) flagged README/docs/app.html — oxfmt formats `.md`/`.html`, but my lefthook pre-commit oxfmt glob only covered `js/ts/json/css`, so those files were committed unformatted and only CI (which checks everything) caught them. Fixed: ran `npm run format` (reformats md/html) and widened the pre-commit oxfmt glob to `*.{js,ts,json,css,md,html}` so local matches CI. Re-pushed.
- Lighthouse (production build): Performance 100, Accessibility 100, Best-Practices 100, SEO 100. CLS was the only weak metric (0.6, from variable card heights + out-of-order card inserts) → fixed with fixed-height cards + stable slot rendering → CLS 0. All DoD Lighthouse targets exceeded.
